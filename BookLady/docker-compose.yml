version: '3.9'

services:
  django:
    image: booklady:0.0.1
    build:
      context: .
      dockerfile: DockerFile
    command: 'python manage.py runserver 0.0.0.0:8000'
    ports:
      - 8000:8000
